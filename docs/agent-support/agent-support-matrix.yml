agent_sessions_version: "5.0-unreleased"
as_of_commit: "ef4e9253"
as_of_date: 2026-08-17
workflow_doc: "docs/agent-support/workflow.md"
notes:
  - "This unreleased snapshot includes Qwen Code support introduced in ef4e9253. It is not yet a release-ledger entry."
  - "Max verified versions come from repo fixtures and documented format changes."
  - "Some agents do not log their own CLI version; those are marked unknown."
  - "Droid is retained for legacy imports and fixtures, but it is excluded from the active session-format monitoring set."
  - "2026-03-31 weekly check: bumped all 7 agents. Claude/Gemini additive schema drift (new keys). Copilot major version (0.0.420->1.0.11): storage layout changed to <uuid>/events.jsonl subdirectory; runtime patched in f77040f, monitoring config updated, new subdirectory fixture added."
  - "2026-04-06 weekly check: bumped OpenCode 1.3.17, Gemini 0.36.0, OpenClaw 2026.4.5 (schema unchanged). Claude 2.1.92 and Copilot 1.0.16: additive new event types (attachment, session.shutdown); schema_drift fixtures updated."
  - "2026-04-12 weekly check: bumped Claude 2.1.92->2.1.104 (attachment type additive, parser handles via .meta, drift test passes), Copilot 1.0.16->1.0.24 (session.shutdown additive, parser handles via default branch, drift test passes), OpenClaw 2026.4.5->2026.4.10 (schema unchanged, fresh session after mass-delete recovery). Prebump drivers failed (claude: --verbose flag needed; copilot: sandbox auth missing) — weekly evidence sufficient."
  - "2026-04-12: added cursor as 8th monitored agent. Initial verified version 2026.04.12 (date-based, no version embedded in transcripts)."
  - "2026-04-16 weekly check: bumped Codex 0.120.0->0.121.0, Claude 2.1.104->2.1.112 (additive stop_hook_summary/permission metadata preserved as .meta), OpenCode 1.4.3->1.4.6 (current storage is opencode.db SQLite; parser already supported SQLite, monitoring/capture updated), and OpenClaw 2026.4.10->2026.4.14 (schema unchanged, fresh local sample). Copilot 1.0.30 and Gemini 0.38.1 remained candidates pending auth-enabled fresh samples."
  - "2026-04-17 follow-up: bumped Gemini 0.37.1->0.38.1 and Copilot 1.0.24->1.0.31 from successful auth-enabled prebump runs; bumped OpenCode 1.4.6->1.4.7 and OpenClaw 2026.4.14->2026.4.15 from fresh local sessions. Gemini prebump now copies non-secret settings/account support files into the sandbox while keeping OAuth credentials under strict hygiene."
  - "2026-04-29 weekly check: bumped Codex 0.121.0->0.125.0, Claude 2.1.112->2.1.123, Gemini 0.38.1->0.40.0, Copilot 1.0.31->1.0.39, OpenCode 1.4.7->1.14.29, and OpenClaw 2026.4.15->2026.4.26. Gemini 0.40.0 moved fresh headless sessions to JSONL under chats/; discovery, parser, capture, monitoring, and fixtures were updated."
  - "2026-04-29 follow-up: added Hermes 0.11.0 canonical session JSON monitoring and excluded Droid from active session-format checks."
  - "2026-05-02 format checks: bumped Codex 0.125.0->0.128.0, Claude 2.1.123->2.1.126, Gemini 0.40.0->0.40.1, OpenCode 1.14.29->1.14.31, and Copilot 1.0.39->1.0.40. Copilot 1.0.40 added normal hook.start/hook.end envelopes; OpenCode 1.14.31 added top-level part.tool metadata. Gemini 0.40.1 requires explicit headless workspace trust for prebump validation. Existing parsers preserve these without UI changes."
  - "2026-05-02 OpenClaw follow-up: OpenClaw 2026.4.29 initially failed during plugin bootstrap because the bundled Telegram plugin could not load ../dist/babel.cjs from the runtime dependency cache. Running the plugin dependency inspection/repair path completed the cache; a fresh local agent session then matched baseline, so OpenClaw is verified at 2026.4.29."
  - "2026-05-07 format checks: bumped Codex 0.128.0->0.129.0, Claude 2.1.126->2.1.132, Gemini 0.40.1->0.41.2, Copilot 1.0.40->1.0.43, OpenCode 1.14.31->1.14.41, and OpenClaw 2026.4.29->2026.5.7. Claude 2.1.132 can add assistant attributionPlugin/attributionSkill metadata; the parser already preserves it as metadata and no UI change is needed. Copilot prebump now accepts COPILOT_GITHUB_TOKEN/GH_TOKEN/GITHUB_TOKEN auth env vars. OpenClaw 2026.5.7 required migrating legacy Telegram streaming config with openclaw doctor --fix before fresh local session evidence could be generated."
  - "Codex support covers local rollout JSONL sessions from Codex CLI, Codex Desktop, and the Codex VS Code extension; these remain one Codex corpus with per-session surface labels. Codex Desktop's sandboxed per-task workspaces (cwd under ~/Documents/Codex/<date>/<slug>/ with work/ and outputs/) are the Codex analogue of Claude Cowork; verified 2026-07-19 that all 83 such threads in ~/.codex/state_5.sqlite point their rollout_path back into ~/.codex/sessions or ~/.codex/archived_sessions, so they need no separate discovery. Their rollout `source` is \"vscode\" while `originator` is \"Codex Desktop\"/\"codex_work_desktop\"; classifyCodexSurface checks originator before source, so they label as desktop, not vscode."
  - "2026-07-19: Claude support covers Cowork (local-agent mode) alongside Claude Code CLI and the Claude Desktop Code tab; these remain one Claude corpus with per-session surface labels. Cowork transcripts are the same Claude JSONL schema under a second root (~/Library/Application Support/Claude/local-agent-mode-sessions/**/local_*/.claude/projects/**), with sidecar local_*.json metadata joined on cliSessionId for title/archive state. Classification is path-first because DB-hydrated sessions carry NULL surface metadata."
  - "2026-05-12: added Pi coding-agent tier-2 local support and weekly session-format monitoring. Evidence is official pi.dev session-format docs, a temporary local 0.74.0 capture under /tmp/as-agent-lab, checked-in fixture Resources/Fixtures/stage0/agents/pi/small.jsonl, focused parser/discovery/resume/probe tests, and a successful Debug build. Live status, analytics, and usage tracking remain unsupported."
  - "2026-05-12 follow-up: bumped Codex 0.129.0->0.130.0, Claude 2.1.132->2.1.140, Copilot 1.0.43->1.0.46, and OpenCode 1.14.41->1.14.48. Fresh Codex and Copilot prebump sessions matched baseline; Claude 2.1.140 matched baseline from a real-home headless transcript after sandbox auth remained unavailable; OpenCode local SQLite evidence matched baseline. No parser, UI, or UX changes were needed."
  - "2026-05-19 format checks: bumped Codex 0.130.0->0.131.0, Claude 2.1.140->2.1.144, Gemini 0.41.2->0.42.0, Copilot 1.0.46->1.0.49, OpenCode 1.14.48->1.15.5, and OpenClaw 2026.5.7->2026.5.18. Fresh prebump or local samples matched baseline after adding OpenClaw custom_message runtime-context coverage to the normal fixture baseline. No parser, UI, or UX changes were needed."
  - "2026-05-28 format check: bumped Codex 0.131.0->0.135.0, Claude 2.1.144->2.1.156, Gemini 0.42.0->0.44.1, Copilot 1.0.49->1.0.55, OpenCode 1.15.5->1.15.12, Hermes 0.11.0->0.15.0, OpenClaw 2026.5.18->2026.5.27, and Pi 0.74.0->0.76.0. Fresh local, capture, prebump, or Hermes state.db evidence matched baseline after adding Pi prebump support, Cursor fixture coverage, OpenClaw trajectory exclusion, Hermes state.db parsing, and weekly-prebump freshness bridging. Claude was verified after the five-hour reset with a successful standard-context Sonnet transcript."
  - "2026-06-02 format check: bumped Codex 0.135.0->0.136.0, Claude 2.1.156->2.1.160, Copilot 1.0.55->1.0.58, OpenCode 1.15.12->1.15.13, Hermes 0.15.0->0.15.1, OpenClaw 2026.5.27->2026.5.28, Cursor 3.5.38->2026.6.2, and Pi 0.76.0->0.78.0. Fresh real-session prebump evidence matched baseline for every configured active agent after adding real-session drivers for OpenCode, OpenClaw, Cursor, and Hermes."
  - "2026-06-03 Cursor follow-up: Cursor CLI latest-source monitoring now uses the official cursor.com/install installer script and Homebrew cursor-cli cask fallback; both identify latest build 2026.06.02-8c11d9f, normalized to 2026.6.2. Cursor Desktop agent-window store.db metadata is monitored by cursor_sqlite_probe with agentId, createdAt, mode, model, and meta-key schema evidence."
  - "2026-06-03 format follow-up: bumped Claude 2.1.160->2.1.161, Gemini 0.44.1->0.45.0, and Copilot 1.0.58->1.0.59. Fresh real-session prebump evidence matched baseline after adding Claude user.promptSource fixture coverage."
  - "2026-06-24 maintenance: active format monitoring moved from removed Gemini CLI support to Antigravity CLI (`agy`) markdown brain artifacts under ~/.gemini/antigravity/brain."
  - "2026-06-24 full format check: bumped Codex 0.136.0->0.142.0, Claude 2.1.161->2.1.190, OpenCode 1.15.13->1.17.9, Copilot 1.0.59->1.0.64, and Pi 0.78.0->0.80.1 from fresh real-session evidence. Pi remains supports_installed_only because upstream is 0.80.2. Antigravity 1.0.11 and OpenClaw 2026.6.9 initially remained blocked by stale/failed prebump evidence; Cursor 2026.6.24 and Hermes 0.17.0 remained upstream candidates pending latest-build validation."
  - "2026-06-26 session-format follow-up: bumped Codex 0.142.0->0.142.2, Claude 2.1.190->2.1.193 (additive user.origin object), and OpenCode 1.17.9->1.17.11 (additive part.compaction type plus message.assistant error/variant and part.text metadata/synthetic keys). Baseline fixtures updated; both parsers preserve unknown families via metadata/default branches, so no parser/UI change. Codex monitoring_broken was a transient codex_status_capture.sh timeout (exit 124), not format drift — fresh 0.142.2 prebump scripts/probe_scan_output/agent_watch/20260626-205841Z-prebump/report.json matched baseline. Antigravity prebump rerun with --allow-real-home stays blocked (antigravity_no_brain_artifact), verified held at 1.0.9. Weekly report scripts/probe_scan_output/agent_watch/20260626-205030Z/report.json reports matches_baseline=True with no unknown types/keys for Codex, Claude, and OpenCode."
  - "2026-06-26 CLI refresh follow-up: updated Copilot->1.0.65 (brew), OpenClaw->2026.6.10 (npm), Cursor->2026.06.24 (install script), and Pi->0.80.2 (npm), then bumped verified Copilot 1.0.64->1.0.65, Cursor 2026.6.2->2026.6.24, and Pi 0.80.1->0.80.2 (OpenClaw already at 2026.6.10). Fresh prebump matched baseline for Copilot (GITHUB_TOKEN forwarded), Pi, and OpenClaw (--allow-real-home). Cursor 2026.06.24 added a roleless turn_ended line ({type, status}) that the role-based fingerprinter buckets under assistant; covered by adding a turn_ended line to the cursor small fixture (parser already handles roleless lines via its fallback branch). Final weekly report scripts/probe_scan_output/agent_watch/20260626-210811Z/report.json reports supports_latest for Codex, Copilot, OpenClaw, Pi, and Hermes, and matches_baseline=True with no unknown types/keys for Cursor."
  - "2026-07-07 weekly format check: bumped Codex 0.142.2->0.142.5 and Claude 2.1.193->2.1.202 (fresh weekly evidence, no schema drift), OpenCode 1.17.11->1.17.13 and Pi 0.80.2->0.80.3 (prebump matched baseline), and Antigravity 1.0.12->1.0.14 and OpenClaw 2026.6.10->2026.6.11 (prebump matched baseline with --allow-real-home after sandbox auth failed). All six matched baseline with no unknown types/keys; no parser, fixture, or UI changes needed. Copilot 1.0.68, Hermes 0.18.0, and Cursor 2026.7.1 remain upstream candidates not yet installed locally. Weekly report scripts/probe_scan_output/agent_watch/20260707-195510Z/report.json; prebump reports 20260707-195618Z-prebump (opencode/pi) and 20260707-200058Z-prebump (antigravity/openclaw)."
  - "2026-06-24 targeted follow-up: updated OpenClaw 2026.6.9->2026.6.10 after openclaw doctor --fix and a fresh local-agent prebump matched baseline; updated Hermes 0.15.1->0.17.0 after hermes update and a fresh state.db prebump matched baseline. Final weekly report scripts/probe_scan_output/agent_watch/20260624-185801Z/report.json reports supports_latest for OpenClaw and Hermes. Antigravity 1.0.11 is current but remains blocked because agy -p answers without creating a monitored brain markdown artifact. Cursor remains verified at installed 2026.6.2 because cursor-agent update and cursor-agent --print are blocked by headless authentication."
  - "2026-07-13 weekly format check: bumped Codex 0.142.5->0.144.3 and Claude 2.1.202->2.1.207 after additive schema drift. Codex 0.144.x adds a `world_state` context-snapshot event (parser resolves it to .meta; added to codex small + schema_drift fixtures with a survives-parsing test). Claude subagent transcripts (~/.claude/projects/**/<parentUUID>/subagents/*.jsonl) add agentId (user/assistant/attachment) and attributionAgent (assistant) keys; added an authentic subagent fixture pair (claude/subagent/<uuid>.jsonl + <uuid>/subagents/agent-*.jsonl with a .meta.json sidecar) so the keys land where they really occur and detectSubagentInfo path-detection is exercised (testClaudeSubagentFixtureDetectsParent). Both drifts are non-breaking (unknown keys ignored, unknown types -> .meta). Bumped OpenCode 1.17.13->1.17.18 and Pi 0.80.3->0.80.6 (prebump scripts/probe_scan_output/agent_watch/20260713-172121Z-prebump/report.json, fresh_matches_baseline=True), and Antigravity 1.0.14->1.0.16 (real-home prebump scripts/probe_scan_output/agent_watch/20260713-173118Z-prebump/report.json, fresh_matches_baseline=True). Usage probes all healthy (codex_status_probe, claude_usage_probe, claude_status ok=true). Weekly report scripts/probe_scan_output/agent_watch/20260713-163616Z/report.json. Held: Copilot 1.0.65 (upstream 1.0.70 not installed), Hermes 0.17.0 (upstream 0.18.2 not installed), Cursor 2026.6.24 (upstream 2026.7.9 not installed), OpenClaw 2026.6.11 (current)."
  - "2026-07-17 weekly format check: bumped Codex 0.144.3->0.144.5 (prebump scripts/probe_scan_output/agent_watch/20260717-061144Z-prebump/report.json, fresh_matches_baseline=True, no drift) and Claude 2.1.207->2.1.211 after additive schema drift. Claude 2.1.211 adds a session-level `mode` event ({type, mode, sessionId}, observed only as mode=normal) alongside the existing permission-mode marker; 3,638 occurrences across 99 local sessions. It is non-breaking: the event carries no content, so role resolves to meta and SessionEventKind.from falls through both switches to its final `return .meta` — it is handled by the fallback, not a named case. Added to claude small + schema_drift fixtures with testClaudeSchemaDriftModeEventSurvivesParsing (passes; 24/24 Stage0GoldenFixturesTests green). NOTE: prebump cannot see this drift — `mode` is interactive-only and the claude_print one-shot driver never emits it, so prebump reported fresh_matches_baseline=True while the weekly scan of real sessions correctly flagged format_drift_detected. Treat a green prebump as necessary but not sufficient for interactive-only event families. Claude prebump required --allow-real-home (Keychain OAuth unavailable in sandbox; apiKeySource=none -> 'Not logged in'). Usage probes all healthy (codex_status_probe, claude_usage_probe, claude_status ok=true). Weekly report scripts/probe_scan_output/agent_watch/20260717-060450Z/report.json; claude prebump 20260717-061321Z-prebump. Held: Copilot 1.0.65 (upstream 1.0.71 not installed), Hermes 0.17.0 (upstream 0.18.2 not installed), Cursor 2026.6.24 (upstream 2026.7.16 not installed). Still blocked_stale_sample pending prebump: OpenCode 1.18.3, OpenClaw 2026.7.1, Pi 0.80.10."
  - "2026-07-22 full format check: bumped Codex 0.144.6->0.145.0, Claude 2.1.215->2.1.217, OpenCode 1.18.3->1.18.4, Pi 0.80.10->0.81.1, and Antigravity 1.0.16->1.1.1. All five matched baseline from fresh real-session prebump (codex/opencode/pi from sandbox; claude/antigravity required --allow-real-home for Keychain/OAuth) with no unknown types/keys; no parser, fixture, or UI change for these. Codex 0.145.0 confirms the ongoing 2026 session-format changes (session_meta gained thread_source/session_id/structured source.subagent; state_5.sqlite thread index relocated to ~/.codex/sqlite/) are already covered — AS reads rollout JSONL, not the SQLite index. FORMAT DRIFT: Copilot 1.0.65 (verified==installed) emits two additive session-level event types, session.auto_mode_resolved ({chosenModel, reasoningBucket, categoryScores, predictedLabel, confidence, candidateModels}) and session.usage_checkpoint ({totalNanoAiu, totalPremiumRequests, modelCacheState}); both use the standard envelope, carry no renderable content, and are preserved via CopilotSessionParser's default branch as .meta. Added both to copilot/schema_drift.jsonl with testCopilotAutoModeAndUsageCheckpointEventsSurviveParsing; Copilot verified stays 1.0.65 (no version change, fixture-only coverage). Usage probes all healthy (codex_status_probe, claude_usage_probe, claude_status ok=true); discovery contracts all pass. Not validated this run: Hermes (driver produced no final response), OpenClaw (auth/config error), Cursor (needs CURSOR_API_KEY, absent from real HOME) — all held at prior verified versions, no evidence of drift. Also hardened scripts/agent_watch.py so an unexecutable agent binary (a botched opencode npm install left a shebang-less stub -> Exec format error) no longer aborts the whole weekly scan (commit f1063c57). Weekly report scripts/probe_scan_output/agent_watch/20260722-042913Z/report.json; prebump reports 20260722-042605Z-prebump (codex), 20260722-043446Z-prebump (all), 20260722-044346Z-prebump (real-home)."
  - "2026-07-22 Cursor follow-up: after logging into the Cursor CLI, cursor-agent auto-updated 2026.6.24->2026.7.20; a fresh --allow-real-home prebump (scripts/probe_scan_output/agent_watch/20260722-050027Z-prebump/report.json) reports ok=True, fresh_matches_baseline=True, unknown_types=[]/unknown_keys={}, is_stale=false. Bumped verified Cursor 2026.6.24->2026.7.20; no drift, no fixture change. Also fixed the crashing ~/.local/bin/cursor shim (set -eu + unguarded $1 on no-arg invocation -> `line 26: $1: unbound variable`); changed \"$1\" to \"${1:-}\". Cursor is now the only earlier auth-blocked agent validated fresh; Hermes and OpenClaw remain held (driver/auth, no drift evidence)."
  - "2026-07-25: added Kimi Code (Moonshot AI `kimi`) tier-2 local support and weekly session-format monitoring, verified at CLI 0.29.1. Storage is JSONL: ~/.kimi-code/sessions/wd_<slug>_<sha256[0:12]>/<sessionId>/agents/<agentId>/wire.jsonl, with a per-session state.json sidecar (title/isCustomTitle/workDir/lastPrompt) and a root session_index.jsonl. Only agents/main is discovered; sibling agent dirs are subagent journals and are excluded. Line 1 is a {type:metadata, protocol_version, created_at} envelope; later lines are flattened ops {type, ...payload, time(epoch ms)}. Conversation content is context.append_message carrying role/content-parts/toolCalls. TWO FACTS ONLY A REAL CAPTURE SHOWED: (1) the installed CLI emits protocol_version 1.4, NOT the 1.5 constant in packages/agent-core-v2/src/wire/migration/migration.ts at source HEAD - monitor the emitted value; (2) config.update carries modelAlias (\"moonshot-ai/kimi-k2.7-code\"), never a bare model key, while llm.request carries both modelAlias and concrete model (\"kimi-k2.7-code\") - a parser reading only `model` yields a nil model for every Kimi session (fixed, regression test testResolvesModelFromModelAliasNotBareModelKey). Session dirs are prefixed session_<uuid> and `kimi -S session_<uuid>` was confirmed against the real CLI to resolve that id. packages/minidb is a bespoke binary Bitcask-style KV store but is gated behind persistence_minidb_readmodel (default:false) and is only a derived read model, so AS deliberately reads wire.jsonl and does NOT implement a minidb reader. Kimi Desktop is an Electron thin client over the same daemon and the same ~/.kimi-code home, so it is a per-session surface label, not a separate source. FIXTURE GAP: the capture account was suspended for insufficient balance (429 provider.rate_limit), so the fixture contains no assistant message and no tool call; the .assistant/.tool_call/.tool_result parser paths remain unexercised by fixture and should be re-captured. No prebump driver yet: kimi -p is a real headless mode so one IS buildable, but drivers are Python classes in DRIVERS in scripts/agent_watch_prebump_drivers.py and that is its own task; weekly local_schema is the drift signal. Analytics, Copy Resume Command, and launch-in-terminal resume are supported; live status and usage/quota tracking remain unsupported. Verified 0.29.2: the CLI auto-updated 0.29.1->0.29.2 mid-session and the live journal still matched the fixture baseline with zero unknown types/keys. One family DID drift and is now covered: permission.set_mode ({mode,time,type}) was absent from the first capture; it is additive, carries no renderable content, resolves to .meta via the parser fallback, and was appended to the fixture baseline with coverage in testNonMessageOpFamiliesSurviveAsMeta. Adding kimi to agent_watch.py required four hardcoded maps (verified_map, two matrix_key maps, and the JSONL-fingerprint agent tuple in _baseline_type_keys_for_agent) - a new JSONL agent is not config-only."
  - "2026-08-01: Kimi fixture gap CLOSED, and closing it exposed a parser defect. CORRECTION to the 2026-07-25 note above: \"Conversation content is context.append_message carrying role/content-parts/toolCalls\" is WRONG - that shape is Kimi's internal Message type, not what the journal emits. Real journals split conversation across TWO op families: context.append_message holds USER TURNS ONLY (23 of 23 ops across seven captures were role:user, none carried toolCalls), while context.append_loop_event holds everything the model produced - {type:content.part, part:{type:text|think}} for assistant text and reasoning, {type:tool.call, toolCallId, name, args, description, display} and {type:tool.result, parentUuid, toolCallId, result:{output[, isError][, note]}} for tools. KimiSessionParser only read context.append_message, so its .assistant/.tool_call/.tool_result branches were unreachable against real data and every Kimi transcript rendered as user turns alone: the new fixture's session surfaced 13 of its 57 real events, and the Analytics message counts added in the Kimi/Pi analytics work counted only those 13. Fixed by parsing loop events (content.part text -> .assistant, think dropped as reasoning per the existing textContent rule; tool.call -> .tool_call with args serialised via sortedKeys; tool.result -> .tool_result, or .error when the NESTED result.isError is true - there is no top-level error flag). The earlier \"account suspended, cannot capture\" blocker was a false negative from checking only context.append_message; no funded re-capture was needed, the material was already on disk. New fixture assistant_tools.jsonl + assistant_tools.state.json is a 200-line redacted authentic capture (6 assistant replies, 19 tool calls over Bash/Agent/Glob/Write/ExitPlanMode/Read/Grep, one isError result, two note results, multi-call steps, plan mode, full compaction); redaction matches small.jsonl (system prompt + tools snapshot trimmed, home/workdir rewritten to the as-agent-lab sandbox path, long payloads clipped). It also extends the weekly baseline with seven op families small.jsonl never had: usage.record, permission.record_approval_result, plan_mode.enter/exit, full_compaction.begin/complete, context.apply_compaction. KNOWN BLIND SPOT: _jsonl_schema_fingerprint keys only on the TOP-LEVEL type, so it cannot see drift inside event.* - a new content.part part type or tool.result key would not be flagged; loop-event internals are covered by KimiSessionParserTests, not by the weekly scan."
  - "2026-08-03: Kimi integration surfaces completed; three defects from the tier-2 merge fixed. CORRECTION to the 2026-07-25 note above: \"launch-in-terminal resume remain unsupported\" is NO LONGER TRUE - Kimi now has the full resume stack (KimiResumeCoordinator/KimiTerminalLauncher/KimiResumeTypes) mirroring Pi, reaching Terminal.app, iTerm2, Warp, and Warp Preview via the shared AgentTerminalLauncher. Policy is sessionThenContinue: `--session <id>` when the CLI advertises it and the session carries an id, else `--continue` for the working directory; there is no `--resume` flag in Kimi, so unlike Pi/Cursor there is no resumeByID strategy and no supportsResume capability to probe. Live status, analytics token accounting, and usage/quota tracking remain unsupported. THE THREE DEFECTS, all of them integration-surface omissions the exhaustive-switch discipline could not catch because each site was a hand-maintained list rather than a switch: (1) NO PREFERENCES PANE - PreferencesTab had no .kimi case, so KimiSessionsRootOverride had three readers (KimiSessionIndexer, AgentEnablement.isAvailable, SessionArchiveManager) and ZERO writers anywhere in the app. Plan Task 7 Step 2 had explicitly required this row; the merge left `// Tier-2: no CLI resume/binary-probe UI for Kimi yet` stubs at three PreferencesView switch sites instead. Now a full Pi-parity pane (PreferencesView+Kimi.swift) with binary Auto/Custom picker, version probe, Check Version/Copy Path/Reveal, availability status, default root, and the storage-root override. (2) NO TOOLBAR FILTER PILL - enabledOtherAgentSpecs in UnifiedSessionsView ended at the Pi arm, so with Kimi enabled there was no pill AND no Agents-overflow entry, leaving includeKimi (default true, persisted at IncludeKimiSessions, fully plumbed through search and the indexer) untogglable; it also undercounted enabledAgentCount so the pills-to-overflow collapse fired one agent late. Kimi takes no keyboard shortcut - Cmd-1..9 are fully allocated (Codex=1, Claude=2 ... Pi=9), same posture as Hermes. (3) HAS-COMMANDS FILTER - the quick filter's JSONL-provider list omitted .kimi, so Kimi sessions fell through to the trailing `return true` and passed the filter with zero tool calls. Predicate extracted to UnifiedSessionIndexer.passesHasCommandsFilter, matching the passesLowMessageVisibilityFilter static beside it, which makes it both testable and exhaustive so the twelfth source cannot repeat this. Also wired KimiSettings into copy-resume, which had hardcoded binaryCommand \"kimi\" and would have made the new binary picker inert. New KimiCLIEnvironment.swift finally lands the file plan Task 8 specified but never created. Coverage: KimiIntegrationSurfaceTests (8) + KimiResumeCoordinatorTests (8); the has-commands regressions were confirmed to fail against the old behaviour before the fix landed."
  - "2026-08-03 full format check: bumped Codex 0.145.0->0.146.0, Claude 2.1.217->2.1.220, OpenCode 1.18.4->1.18.11, Pi 0.81.1->0.83.0, all from fresh prebump. Also bumped Copilot 1.0.65->1.0.77 after fixing its prebump driver (it passed the deprecated --config-dir and its declared credential file ~/.copilot/hosts.json no longer exists, so with no GITHUB_TOKEN exported it had no auth path at all; now sets COPILOT_HOME and falls back to `gh auth token`). The working driver immediately found real 1.0.77 drift: rte/encryptedContent on message+tool events, interactionId on system.message, and eight new auto-router telemetry keys - all additive. Claude also moved to nested fingerprinting, exposing its usage struct (cache_creation, server_tool_use, iterations, service_tier, inference_geo, speed) and ten attachment subtypes; that work exposed a walker defect where only the FIRST dict of a list was sampled, hiding Claude's heterogeneous message.content blocks. HELD: OpenClaw 2026.6.11 (missing-provider-auth; shares Codex OAuth store, real-HOME gives refresh_token_reused 401), Antigravity 1.1.1 (only rich session predates the 1.1.10 upgrade), Hermes 0.17.0 (thin sample). PARSER FIX: Antigravity CODE_ACTION/SEARCH_WEB now render as .tool_result instead of system .meta, and truncated_fields is surfaced instead of silently dropped. PRICING FIX: codex-auto-review had no price key and bills real tokens, which DELETED whole sessions from the $ runway (any unpriceable contributing slice nils the session); added at gpt-5.6/sol rates to prices.json + bundledJSON. MONITORING REBUILD: the fingerprint was blind to every nested payload (codex fingerprinted as {payload,timestamp,type}), so the token_count/rate_limits usage channel monitoring.md claimed to watch was never actually watched; added nested fingerprinting at depth 3 with opaque-key and discriminator-depth guards (codex patch_apply_end.changes is keyed by absolute file path and was leaking real user paths into reports). Weekly now unions the newest 5 sessions - single-session sampling let a 4-line prebump hello mask antigravity drift entirely - with a blocked_thin_sample verdict gated on low coverage AND low volume. Baseline convention fixed: verified-and-handled types go in the normal fixture, not schema_drift.jsonl, which is why Copilot's two 2026-07-22 types had re-reported as drift every week since. codex/small.jsonl and copilot/small.jsonl rebuilt at modern shape; 1837 tests pass. Weekly scripts/probe_scan_output/agent_watch/20260803-211811Z/report.json."
  - "2026-08-13 full format check + GROK ADDED AS THE 11TH MONITORED AGENT. Grok shipped in the app with zero agent_watch coverage; it now has a weekly fingerprint that treats a session as the DIRECTORY it is (chat_history.jsonl nested to depth 3, merged with the sibling summary.json as a `summary` bucket), a grok-build Homebrew cask upstream source, and a grok_update_check probe. BUMPED: Codex 0.146.0->0.147.0, Copilot 1.0.77->1.0.79, Cursor 2026.7.20->2026.8.11, Antigravity 1.1.1->1.1.12 (held since 2026-08-03), OpenCode 1.18.11->1.18.16, Pi 0.83.0->0.84.1, Kimi 0.31.1->0.34.0, OpenClaw 2026.6.11->2026.7.1. Claude stays 2.1.220 (upstream 2.1.232 is not installed); Hermes stays 0.17.0. Every agent now reports unknown_types=[] and unknown_keys={}. THE PREBUMP EARNED ITS KEEP TWICE: a fresh stable-0.147.0 codex session emitted world_state.state.orchestrator_skills, multi_agent_usage_hint and multi_agent_mode.usage_hint_hash that appear in ZERO of the 1184 sessions on disk (including 33 on the immediately-prior alpha); and building Kimi's first-ever prebump driver exposed profile.bind + turn.ended + event.step.end{providerFinishReason,rawFinishReason} present in EVERY kimi session, which blocked_stale_sample had been hiding because staleness short-circuits the schema verdict. INSTRUMENT FIXES: (1) a passing prebump replaces the weekly schema_diff with its own one-prompt session, and the thin-sample gate scored only that - so codex reported blocked_thin_sample off a 20-event prebump while its weekly union held 2808 clean events; thinness is now judged across every sample and pinned by tests. (2) real_home_session prebump sessions land in the real store and enter the newest-5 window, so 'Say hello' prompts were actively degrading the next weekly sample (two of them dropped antigravity to 24 events and a genuine blocked_thin_sample); antigravity/cursor/openclaw now use a tool-using prompt, hermes was left alone because it fails with either prompt. (3) Antigravity finally has an upstream source - its own auto-updater endpoint, extracted from the agy binary, which answers 'Stable Version: 1.1.12'; latest_unknown was never a property of the agent, just a missing config. NO PARSER CHANGES WERE NEEDED: claude thinking_tokens is a SUBSET of output_tokens (verified on 143 records) so the Runway is right to ignore it; antigravity GENERIC, kimi profile.bind/turn.ended and codex world_state all resolve to .meta by design. ONE LATENT RISK LOGGED: the Copilot parser never reads data.messageId, so if Copilot ever emits chunkCount>1 the fragments will render as separate assistant bubbles (every record on disk is 0/1 today). OPENCLAW IS BROKEN FOR THE USER, NOT JUST FOR US: its only model routes through an agentRuntime 'codex' harness whose @openclaw/codex plugin is not installed, on top of an expired openai:default token. 1897 Swift + 138 python tests pass. Weekly scripts/probe_scan_output/agent_watch/20260814-000832Z/report.json."
agents:
  codex_cli:
    max_verified_version: "0.147.0"
    version_field: "payload.cli_version"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/codex/small.jsonl"
      - "Resources/Fixtures/stage0/agents/codex/large.jsonl"
      - "Resources/Fixtures/stage0/agents/codex/schema_drift.jsonl"
  claude_code:
    max_verified_version: "2.1.220"
    version_field: "version"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/claude/small.jsonl"
      - "Resources/Fixtures/stage0/agents/claude/large.jsonl"
      - "Resources/Fixtures/stage0/agents/claude/schema_drift.jsonl"
      - "Resources/Fixtures/stage0/agents/claude/subagent/0a1b2c3d-4e5f-4061-8071-2a3b4c5d6e7f.jsonl"
      - "Resources/Fixtures/stage0/agents/claude/subagent/0a1b2c3d-4e5f-4061-8071-2a3b4c5d6e7f/subagents/agent-a0ad4f44468bdf20d.jsonl"
  antigravity:
    max_verified_version: "1.1.12"
    version_field: "not_logged"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/antigravity/small.md"
      - "Resources/Fixtures/stage0/agents/antigravity/cli_small.jsonl"
      - "Resources/Fixtures/stage0/agents/antigravity/cli_schema_drift.jsonl"
  copilot_cli:
    max_verified_version: "1.0.79"
    version_field: "data.copilotVersion"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/copilot/small.jsonl"
      - "Resources/Fixtures/stage0/agents/copilot/large.jsonl"
      - "Resources/Fixtures/stage0/agents/copilot/schema_drift.jsonl"
      - "Resources/Fixtures/stage0/agents/copilot/subdir_v1/aaaabbbb-1111-2222-3333-ccccddddeeee/events.jsonl"
  opencode:
    max_verified_version: "1.18.18"
    version_field: "session.version"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/session/proj_test/ses_s_stage0_small.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/session/proj_test/ses_s_stage0_large.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/message/s_stage0_small/msg_0001.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/message/s_stage0_small/msg_0002.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/message/s_stage0_large/msg_0001.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/message/s_stage0_large/msg_0002.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_user_small/001.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_small/001.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/001.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/002.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/003.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/004.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/005.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/006.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_v2/part/m_assistant_large_1/007.json"
      - "Resources/Fixtures/stage0/agents/opencode/storage_legacy/session/proj_test/ses_s_stage0_drift.json"
  hermes:
    max_verified_version: "0.17.0"
    version_field: "not_logged"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/hermes/small.json"
      - "Resources/Fixtures/stage0/agents/hermes/large.json"
      - "Resources/Fixtures/stage0/agents/hermes/schema_drift.json"
  openclaw:
    max_verified_version: "2026.7.1"
    version_field: "not_logged"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/openclaw/small.jsonl"
      - "Resources/Fixtures/stage0/agents/openclaw/large.jsonl"
      - "Resources/Fixtures/stage0/agents/openclaw/schema_drift.jsonl"
  cursor:
    max_verified_version: "2026.8.11"
    version_field: "not_logged"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/cursor/small.jsonl"
      - "Resources/Fixtures/stage0/agents/cursor/large.jsonl"
      - "Resources/Fixtures/stage0/agents/cursor/schema_drift.jsonl"
      - "Resources/Fixtures/stage0/agents/cursor/subagent/parent.jsonl"
      - "Resources/Fixtures/stage0/agents/cursor/subagent/subagents/child.jsonl"
  pi:
    max_verified_version: "0.84.2"
    version_field: "not_logged"
    support_scope: "tier-2 local JSONL transcript discovery, browsing, search, Preferences controls, colors, resume/copy-resume commands, analytics, and weekly session-format monitoring"
    unsupported_surfaces:
      - "live/active status"
      - "usage/rate-limit tracking"
      - "subagent hierarchy"
    evidence_docs:
      - "https://pi.dev/docs/latest/quickstart"
      - "https://pi.dev/docs/latest/usage"
      - "https://pi.dev/docs/latest/session-format"
      - "https://pi.dev/docs/latest/settings"
    evidence_captures:
      - "/tmp/as-agent-lab/pi-agent/sessions/2026-05-12T01-02-27-657Z_019e19b4-eb48-746a-aa6b-8dfcfa37954b.jsonl"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/pi/small.jsonl"
    evidence_tests:
      - "AgentSessionsTests/PiSessionParserTests.swift"
      - "AgentSessionsTests/PiResumeCommandBuilderTests.swift"
      - "AgentSessionsTests/PiCLIEnvironmentTests.swift"
  kimi_code:
    max_verified_version: "0.36.1"
    version_field: "not_logged"
    support_scope: "tier-2 local JSONL transcript discovery, browsing, search, Preferences controls, colors, copy-resume command, launch-in-terminal resume, analytics, and weekly session-format monitoring"
    unsupported_surfaces:
      - "live/active status"
      - "usage/rate-limit tracking"
      - "subagent hierarchy"
    evidence_docs:
      - "https://github.com/MoonshotAI/kimi-code"
      - "https://www.kimi.com/code/docs/en/kimi-code-cli/guides/getting-started.html"
    evidence_captures:
      - "~/.kimi-code/sessions/wd_project_ac3bb318f98e/session_9eb1bf57-c1af-48a5-b658-0e8d9fe794f5/"
      - "~/.kimi-code/sessions/wd_codex-history_205016864bd1/session_1bcbe023-4f7e-4971-9dc9-43b8756045b0/"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/kimi/small.jsonl"
      - "Resources/Fixtures/stage0/agents/kimi/state.json"
      - "Resources/Fixtures/stage0/agents/kimi/assistant_tools.jsonl"
      - "Resources/Fixtures/stage0/agents/kimi/assistant_tools.state.json"
      - "Resources/Fixtures/stage0/agents/kimi/subagent_agent-0.jsonl"
    evidence_tests:
      - "AgentSessionsTests/KimiSessionParserTests.swift"
      - "AgentSessionsTests/KimiSessionDiscoveryTests.swift"
      - "AgentSessionsTests/KimiResumeCommandBuilderTests.swift"
  grok_cli:
    max_verified_version: "1.0.4"
    version_field: "not_logged"
    support_scope: "tier-2 local JSONL transcript discovery, browsing, search, Preferences controls, colors, copy-resume command, launch-in-terminal resume, analytics, and subagent hierarchy"
    unsupported_surfaces:
      - "live/active status"
      - "usage/rate-limit tracking"
    notes:
      - "Prebump real-session validation added 2026-08-17 (driver `grok_single`), which is what lifted Grok from a permanent supports_installed_only cap to supports_latest. It runs `grok -p` headless with a pinned --session-id inside a sandbox HOME and a sandbox --cwd, so the percent-encoded workdir bucket is predictable and no real repository is ever the session's cwd. Grok exposes no api-key env var (only GROK_SANDBOX), so auth is always the credential-file copy of ~/.grok/auth.json - an OAuth record carrying account PII alongside the token, hence credential_file with the strict 0600 gate, never support_file. The driver verifies the sibling summary.json exists before reporting ok: a Grok session is a directory, and returning a transcript whose sidecar never landed would hand the fingerprinter half a session and surface the gap as schema drift."
      - "Weekly agent_watch monitoring added 2026-08-13: nested fingerprint over <session>/chat_history.jsonl merged with the sibling summary.json (bucket `summary`), plus a `grok_update_check` probe for authoritative latest. Latest source is the grok-build Homebrew cask JSON — Grok ships a native x.ai binary, not npm."
      - "Subagent hierarchy resolves from the parent's tree, not the child's sidecar: a spawned run is written both as a top-level session directory and as <parent>/subagents/<childId>/meta.json, which is the only place parent_session_id and subagent_type appear. Verified across 9 subagent links at 1.0.3."
      - "Grok's own `grok sessions list` hides subagent sessions; Agent Sessions surfaces them with parentage."
      - "Nesting was not observed: a subagent asked to delegate produced further subagents recorded under the root session, and no child owned its own subagents/ tree."
      - "Not yet exercised against real captures: the truncated-preview count fallback (no session has reached the 200-line preview cap; the largest seen is 167 lines) and the compaction/ subtree."
    evidence_docs:
      - "grok --help at 1.0.3 (1a29d5bc12d4)"
    evidence_captures:
      - "~/.grok/sessions/<percent-encoded workdir>/<sessionId>/ (141 sessions surveyed at 1.0.0; 13 sessions across 2 workdir buckets at 1.0.3, including 9 subagents, web-search backend_tool_call, and an image content part)"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/grok/chat_history.jsonl"
      - "Resources/Fixtures/stage0/agents/grok/summary.json"
      - "Resources/Fixtures/stage0/agents/grok/subagent/chat_history.jsonl"
      - "Resources/Fixtures/stage0/agents/grok/subagent/summary.json"
    evidence_tests:
      - "AgentSessionsTests/GrokSessionParserTests.swift"
      - "AgentSessionsTests/GrokSessionDiscoveryTests.swift"
    evidence_tools:
      - "scripts/grok_chat_schema_probe.py"
  qwen_code:
    max_verified_version: "0.14.3"
    version_field: "record.version"
    support_scope: "tier-2 local JSONL transcript discovery, browsing, search, Preferences controls, colors, saved-session backfill, active-session copy-resume command and launch-in-terminal resume, analytics, and weekly session-format monitoring"
    unsupported_surfaces:
      - "live/active status"
      - "usage/rate-limit tracking"
      - "images"
      - "subagent hierarchy"
      - "automatic QWEN_RUNTIME_DIR discovery (choose that runtime directory's projects folder in Settings)"
      - "resume from an arbitrarily renamed copied projects root (those transcripts remain browseable and searchable)"
      - "fresh authenticated 0.21.13 transcript and live resume validation"
      - "prebump real-session validation (no agent_watch prebump driver yet; weekly monitoring is configured)"
    notes:
      - "The installed Qwen Code CLI is 0.21.13. Its local package writer still uses projects/<project>/chats/<session-id>.jsonl and chats/archive; its own filename predicate accepts 32-36 hexadecimal-or-hyphen IDs. The user/assistant/tool_result record families are represented by synthetic fixtures. The installed help advertises --resume <id> and --continue, but its loader resolves active chats only, so archived histories are deliberately not offered resume. Model execution was authentication-blocked, so no fresh 0.21.13 transcript or end-to-end resume is claimed."
      - "THE AUTH BLOCK HAS A NAMED CAUSE AND IT IS NOT FIXABLE BY RE-LOGIN (confirmed 2026-08-17 by running `qwen -p`): 'Qwen OAuth free tier was discontinued on 2026-04-15. Run /auth to switch to Coding Plan, OpenRouter, Fireworks AI, or another provider.' That date is why the newest transcript on disk is 2026-04-13 and why ~/.qwen/oauth_creds.json expired 2026-04-19 - the account did not lapse, the tier was withdrawn. `qwen auth` is also gone as a subcommand in 0.21.x (it reports '(removed)'); provider selection is the interactive /auth flow. CONSEQUENCES, so nobody re-derives this: qwen will sit at blocked_stale_sample indefinitely, no 0.21.13 transcript can be captured, resume cannot be validated end-to-end, and no prebump driver can be built until a paid Coding Plan or an alternate provider (OpenRouter/Fireworks) is configured. That is a billing decision for the maintainer, not an engineering task."
      - "First weekly agent_watch run (2026-08-17) reported format_drift_detected, and it was an incomplete baseline rather than upstream movement: the five synthetic fixtures never modelled the systemPayload telemetry surface real transcripts carry (outputHistoryItems/systemInfo from /about, uiEvent, phase, rawCommand) or the assistant contextWindowSize and envelope gitBranch keys. Closed by sweeping both on-disk sessions with rebuild_stage0_baseline.py into system_telemetry.jsonl. uiEvent.function_args is registered opaque in _NESTED_OPAQUE_KEYS: it is the telemetry echo of a tool's own parameter object, so walking it would make every new tool parameter read as Qwen drift. Qwen was also missing from rebuild_stage0_baseline.py's MATRIX_KEY map (its section is qwen_code:), the same trap grok hit on 2026-08-13, which would have reported the entire schema missing and then failed --emit."
      - "The retained local transcript has 32 records and logs version 0.14.3. It was inspected only for keys, types, counts, and non-sensitive metadata; no production prompt, output, account identifier, project path, URL, credential, or record was copied."
      - "QWEN_HOME is honored automatically by both discovery and availability. QWEN_RUNTIME_DIR can relocate session output but is intentionally not read from the app process environment because it may be scoped only to the launched CLI; users must select its literal projects folder as the custom storage root. Copy and launch commands then carry the shell-quoted runtime parent through QWEN_RUNTIME_DIR; default-root commands remain unchanged. Arbitrarily renamed copied roots are browse-only."
      - "Current-writer systemPayload.displayText takes precedence over hook context appended to message.parts. For retained payload-less records, the current reader's exact final-part wrapper validation removes only a well-formed qwen:user-prompt-submit-context block; malformed or nested lookalikes remain visible. goal_runtime, cron, and notification user-role envelopes remain metadata rather than inflating user-turn counts or titles."
      - "Discovery and resume share an exact root-relative location invariant: only projectsRoot/<project>/chats/<id>.jsonl is active and projectsRoot/<project>/chats/archive/<id>.jsonl is archived. Nested chats lookalikes and Agent Sessions archive fallback copies are never offered resume."
      - "Current-reader compatibility reconstructs the last non-artifact parent chain, aggregates repeated-UUID fragments in physical order, recovers multiple complete JSON objects glued on one physical line, and derives the latest active-chain cwd and custom_title. Dead rewind branches and trailing artifacts do not enter the transcript, title, counts, search metadata, or resume directory."
    evidence_captures:
      - "one retained local 0.14.3 transcript (schema-only inspection; 32 records)"
    evidence_fixtures:
      - "Resources/Fixtures/stage0/agents/qwen/019f0000-0000-7000-8000-000000000001.jsonl"
      - "Resources/Fixtures/stage0/agents/qwen/019f0000-0000-7000-8000-000000000002.jsonl"
      - "Resources/Fixtures/stage0/agents/qwen/019f0000000070008000000000000003.jsonl"
      - "Resources/Fixtures/stage0/agents/qwen/019f0000-0000-7000-8000-000000000004.jsonl"
      - "Resources/Fixtures/stage0/agents/qwen/019f0000-0000-7000-8000-000000000005.jsonl"
      - "Resources/Fixtures/stage0/agents/qwen/system_telemetry.jsonl"
    evidence_tests:
      - "AgentSessionsTests/QwenIntegrationTests.swift"
  devin_cli:
    max_verified_version: "3000.3.27 (0becb483)"
    version_field: "sessions.db records (verified via scripts/devin_sessions_schema_probe.py against the installed CLI)"
    support_scope: "tier-2 identity-backed SQLite transcript discovery, browsing, search, Preferences controls, colors, saved-session backfill, active-session copy-resume command and launch-in-terminal resume, and analytics"
    unsupported_surfaces:
      - "live/active status"
      - "images"
      - "archive (Devin stores only active sessions in the sessions.db; archived history is not retained by the CLI)"
      - "custom storage root with an arbitrary structure (the devin data directory, cli directory, or the sessions.db file itself are accepted)"
      - "weekly session-format monitoring (devin is not yet in the monitored agent set)"
    notes:
      - "The installed Devin CLI is 3000.3.27 (0becb483). Storage is a single shared SQLite database at ~/.local/share/devin/cli/sessions.db; the reader parses the main-chain message tree (user/assistant/tool_result records) and derives cwd from the state.json sidecar. resume --help advertises -c/--continue and -r/--resume <id>; session ids are human-readable slugs (sessions.id primary key) passed through verbatim."
      - "Model execution was authentication-blocked, so no fresh end-to-end resume is claimed; the copied/launched command plans are tested against probe-advertised capabilities, and installed help and reader behavior justify the explicitly untested resume integration."
      - "Devin is identity-backed and DB-only: parseFullByPath is nil, parseFullByIdentity is used, and no filesystem archives exist. The source is excluded from image browsing because the DB records carry no inline images."
    evidence_tests:
      - "AgentSessionsTests/DevinSqliteReaderTests.swift"
      - "AgentSessionsTests/DevinSessionDiscoveryTests.swift"
      - "AgentSessionsTests/DevinIntegrationSurfaceTests.swift"
    evidence_tools:
      - "scripts/devin_sessions_schema_probe.py"
