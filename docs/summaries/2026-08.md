# August 2026

- Added Devin CLI as the fourteenth local session source, read from the shared `sessions.db` SQLite database under the CLI data directory, with main-chain transcript rendering, search and Analytics participation, Settings controls (binary, storage root), saved-session backfill, and resume commands for active sessions based on installed help/reader evidence and hermetic tests. Devin keeps only active sessions in the database, so there is no archived-history surface; the records carry no inline images; resume remains end-to-end untested because authentication blocked a disposable run.
- Added Qwen Code as the thirteenth local session source, with active-chain JSONL reconstruction, tolerant glued-record recovery, search and Analytics participation, Settings controls, saved-session backfill, and resume commands for active chats based on installed help/reader evidence and hermetic tests. Archived Qwen histories remain browse-only; active-chat resume remains end-to-end untested because authentication blocked a disposable 0.21.13 run.
- Added a one-time, dismissible in-app invitation asking long-retained users to help add support for another coding agent, shown as the lowest-priority card in the session-list top slot and linking to the source-proposal form and the contributing guide. It becomes eligible after 25 opened sessions or 45 days, never appears on a fresh-install launch or when another card already holds the slot, and terminates on dismissal, a second "Maybe later", three ignored launches per round, or on opening either page. The card reads no session data and transmits nothing; its copy carries the standing warning never to share real transcripts, keys, or private paths.
- Added a manual contribution path for new coding-agent sources, including a source proposal form, privacy-safe evidence requirements, an AI-agent implementation brief, and a capability-focused PR template.
- Added the Session-Bench v0.3 leaderboard poster to the launch article and a compact download section on the benchmark page.
- The "Live sessions only" filter now keeps running Claude Desktop chats visible by reusing Claude Session Runway identities as live Desktop presences, including active/idle state and claimed transcript de-duplication.
- Codex usage recovery now clears stale in-memory fetch cooldowns and retries silently before diagnosing an authentication problem, so upgrading a plan can refresh with the existing login instead of requiring an app restart or opening the Fix dialog.
- The usage **Fix…** flow only presents `codex login` after a fresh request is authoritatively rejected.
- Fixed the Quota Meter's Session Runway transitions so manual lens changes and automatic quota-window fallbacks update rows to the effective unit immediately instead of leaving stale values visible during recalculation.
- Corrected the 4.7 appcast and support matrix to describe Kimi as the tenth current source with terminal resume support.
- Database-backed OpenCode and Hermes sessions now carry stable identity, storage path, and content revision through background search ingest, including WAL-only updates, paged stale-hit rejection, transient-read protection, root moves, and complete archive lifecycle cleanup.
- Corrected OpenClaw product copy to match the app: its sessions support browsing, search, images, and recovery, but not resume commands.
